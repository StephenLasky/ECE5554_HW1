function im = gaussian_filter( im )
%UNTITLED2 Summary of this function goes here
%   Detailed explanation goes here

sigma = 2
f_width = 5 % we assume this is an odd number

[rows,cols] = size(im);
new_im = zeros(rows,cols, 'single');

for row = 1:rows
    for col = 1:cols
        summation = 0.0;
        
        for f_row = 1:f_width
            for f_colw = 1:f_width
                
            end
        end
        
        
        
    end
end





end

